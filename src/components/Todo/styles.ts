import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    flex: 1,
    flexDirection: 'row',
  },
  text: {
    marginTop: 7,
    marginRight: 3,
  },
});

export default styles;
