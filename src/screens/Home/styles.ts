import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    width: '100%',
    flex: 1,
  },
  todoListContainer: {
    width: '100%',
    flex: 1,
  },
});

export default styles;
